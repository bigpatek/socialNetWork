let initialState = {
    friends: [
        {id: '1', name: 'Артём'},
        {id: '2', name: 'Ваня'},
        {id: '3', name: 'Петя'}
    ]
}

const sidebarReducer = (state = initialState, action) => {

    return state;
}

export default sidebarReducer;